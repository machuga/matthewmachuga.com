---
title: Blog
pageable: true
per_page: 10
---

.row
  .col-md-12
    - if paginate && num_pages > 1
      %p
        Page #{page_number} of #{num_pages}
      - if prev_page
        %p= link_to 'Previous page', prev_page
  .col-md-12
    - page_articles.each_with_index do |article, i|
      %article.article
        %h2
          = link_to article.title, article
          %span.publish-date= article.date.strftime('%b %e, %Y')
        %blockquote.description= article.summary(250)
  .col-md-12
    - if paginate
      - if next_page
        %p= link_to 'Next page', next_page
