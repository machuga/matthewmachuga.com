---
title: Archive
pageable: true
per_page: 10
---

<section class="tags">
  <h1>
    Find Articles by Tags (#{articles.size})
  </h1>
  <ul>
    <% blog.tags.each do |tag, articles| %>
      <li>
        <%= link_to tag, tag_path(tag) %>
      </li>
    <% end %>
  </ul>
</section>

<section class="years">
  <h1>
    Year (#{articles.size})
  </h1>
  <ul>
    <% blog.articles.group_by { |a| a.date.year }.each do |year, articles| %>
      <li>
        <%= link_to year, blog_year_path(year) %>
      </li>
    <% end %>
  </ul>
</section>
